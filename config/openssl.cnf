# Custom OpenSSL configuration to enable legacy provider for this application.
# This allows handling of certificates created with older, insecure algorithms.

openssl_conf = openssl_init

[openssl_init]
providers = provider_sect

[provider_sect]
default = default_sect
legacy = legacy_sect

[default_sect]
activate = 1

[legacy_sect]
activate = 1
